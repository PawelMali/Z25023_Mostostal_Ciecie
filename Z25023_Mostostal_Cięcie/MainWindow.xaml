<Window x:Class="Z25023_Mostostal_Cięcie.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Z25023_Mostostal_Cięcie"
        xmlns:ui="clr-namespace:Z25023_Mostostal_Cięcie.UI"
        mc:Ignorable="d"
        Title="Symulator Prasy Wykrawającej - Hybrid Multistep" Height="768" Width="1024"
        WindowStartupLocation="CenterScreen">
    
    <Grid Background="#F5F5F5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" Margin="10" Padding="15" 
                BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="5">
            <StackPanel>
                <TextBlock Text="Parametry Detalu" FontWeight="Bold" FontSize="16" Margin="0,0,0,15" Foreground="#333"/>

                <TextBlock Text="Długość (L) [mm]:" Margin="0,0,0,5"/>
                <TextBox Name="txtLength" Text="993,0" Margin="0,0,0,10" Padding="4"/>

                <TextBlock Text="Skok (P) [mm]:" Margin="0,0,0,5"/>
                <ComboBox Name="cmbPitch" Margin="0,0,0,10" Padding="4">
                    <ComboBoxItem Content="11,1" />
                    <ComboBoxItem Content="22,2" />
                    <ComboBoxItem Content="33,3" IsSelected="True" />
                    <ComboBoxItem Content="44,4" />
                    <ComboBoxItem Content="55,5" />
                    <ComboBoxItem Content="66,6" />
                    <ComboBoxItem Content="77,7" />
                    <ComboBoxItem Content="88,8" />
                    <ComboBoxItem Content="99,9" />
                    <!--<ComboBoxItem Content="122,1" />-->
                </ComboBox>

                <TextBlock Text="Opcje technologiczne:" Margin="0,15,0,5" FontWeight="SemiBold"/>
                <CheckBox x:Name="chkEnableSerration" Content="Włącz Seratację (Tryb Progresywny)" IsChecked="False" Margin="0,0,0,15" FontWeight="Bold" Foreground="DarkGreen" />

                <TextBlock Text="Typ profilu matrycy (Cięcie):" Margin="0,5,0,5" FontWeight="SemiBold"/>
                <ComboBox Name="cmbCuttingType" Margin="0,0,0,10" Padding="4">
                    <ComboBoxItem Content="Typ P (TFT 1, 3, 4, 5)" Tag="P" IsSelected="True"/>
                    <ComboBoxItem Content="Typ T (TFT 2, 6)" Tag="T"/>
                </ComboBox>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                        <TextBlock Text="Margines L:" Margin="0,0,0,5"/>
                        <TextBox Name="txtMarginLeft" Text="30,3" Padding="4"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="5,0,0,0">
                        <TextBlock Text="Margines P:" Margin="0,0,0,5"/>
                        <TextBox Name="txtMarginRight" Text="30,3" Padding="4"/>
                    </StackPanel>
                </Grid>

                <Separator Margin="0,10,0,15"/>

                <TextBlock Text="Zakres Cięcia Nożyc" FontWeight="Bold" FontSize="16" Margin="0,0,0,15" Foreground="#333"/>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                        <TextBlock Text="Min [mm]:" Margin="0,0,0,5"/>
                        <TextBox Name="txtShearMin" Text="1850,7" Padding="4"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="5,0,0,0">
                        <TextBlock Text="Max [mm]:" Margin="0,0,0,5"/>
                        <TextBox Name="txtShearMax" Text="2650,7" Padding="4"/>
                    </StackPanel>
                </Grid>

                <Button Name="RunSimulationButton" Content="URUCHOM SYMULACJĘ" Height="40" 
                        Background="#007ACC" Foreground="White" FontWeight="Bold" Margin="0,0,0,10"
                        Click="RunSimulationButton_Click"/>

                <Button Content="SKANER BŁĘDÓW (FUZZ TEST)" Background="DarkOrange" Foreground="White" FontWeight="Bold" Margin="0,10,0,0" Click="RunFuzzTestButton_Click"/>


            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="0,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#E1F0FA" BorderBrush="#B6D4EA" BorderThickness="1" 
                    CornerRadius="5" Padding="10" Margin="0,0,0,10">
                <TextBlock Name="SummaryTextBlock" FontSize="14" Foreground="#004A7F">
                    <Run Text="Brak wyników." FontWeight="Bold"/> 
                    <Run Text=" Uruchom symulację, aby zobaczyć podsumowanie: Ilość otworów, Wartości marginesów, Zakres perforacji."/>
                </TextBlock>
            </Border>

            <TabControl Grid.Row="1" Background="White" BorderBrush="#DDDDDD">

                <TabItem Header="Terminal Wyników (Tabela)">
                    <DataGrid Name="ResultsDataGrid" AutoGenerateColumns="False" 
                              IsReadOnly="True" HeadersVisibility="Column" 
                              GridLinesVisibility="Horizontal" AlternatingRowBackground="#F9F9F9">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="KROK" Binding="{Binding StepNumber}" Width="60"/>

                            <DataGridTextColumn Header="PRZESUW Δ (mm)" Binding="{Binding StepDisplacement, StringFormat={}{0:N2}}" Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="ABS X (mm)" Binding="{Binding Delta, StringFormat={}{0:N2}}" Width="100" Foreground="Gray"/>

                            <DataGridTextColumn Header="OŚ NOŻA (mm)" Binding="{Binding CutTargetX, StringFormat={}{0:N2}}" Width="120" Foreground="Magenta"/>

                            <DataGridCheckBoxColumn Header="CIĘCIE" Binding="{Binding IsCutActive}" Width="70"/>

                            <DataGridTextColumn Header="STEMPLE (MASK)" Binding="{Binding PunchesMask}" Width="150"/>
                            <DataGridTextColumn Header="INFO" Binding="{Binding Info}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>

                <TabItem Header="Wizualizacja Kaskadowa">
                    <Grid Background="#F0F0F0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,10,10,0">
                            <TextBlock Text="Powiększenie schematu:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
                            <Slider x:Name="ZoomSlider" Minimum="0.1" Maximum="2.0" Value="0.5" Width="200" 
                                    VerticalAlignment="Center" TickFrequency="0.1" IsSnapToTickEnabled="True" TickPlacement="BottomRight"/>
                            <TextBlock Text="{Binding Value, ElementName=ZoomSlider, StringFormat=\{0:N1\}x}" 
                                       VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="Bold" Foreground="#005A9E"/>
                        </StackPanel>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Margin="0,10,0,0">
                            <ItemsControl Name="VisualizationItemsControl" Margin="10">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </TabItem>

                <TabItem Header="Zgeneralizowana Pętla (Algorytm)">
                    <Grid Background="White" Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="#E6F2FA" BorderBrush="#0078D7" BorderThickness="1" Padding="10" Margin="0,0,0,10">
                            <TextBlock TextWrapping="Wrap" FontSize="13">
                                <Run FontWeight="Bold" Foreground="#005A9E" Text="ZGENERALIZOWANY ALGORYTM PRODUKCYJNY: "/>
                                <Run Text="Poniższa sekwencja kroków to gotowy podprogram. Aby wyprodukować N płaskowników, "/>
                                <Run FontWeight="Bold" Text="powtarzaj tę tabelę N razy."/>
                                <LineBreak/>
                                <Run Text="UWAGA: " Foreground="Red" FontWeight="Bold"/>
                                <Run Text="Tylko przy pierwszym wykonaniu pętli (start maszyny) użyj wartości z kolumny "/>
                                <Run FontWeight="Bold" Text="'PRZESUW STARTOWY' "/>
                                <Run Text="(wynosi 0.00). We wszystkich kolejnych iteracjach używaj wyłącznie kolumny "/>
                                <Run FontWeight="Bold" Text="'PRZESUW W PĘTLI'."/>
                            </TextBlock>
                        </Border>

                        <DataGrid x:Name="LoopSequenceDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                                  CanUserSortColumns="False" AlternatingRowBackground="#F9F9F9" GridLinesVisibility="Horizontal">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="KROK PĘTLI" Binding="{Binding LoopIndex}" FontWeight="Bold"/>
                                <DataGridTextColumn Header="PRZESUW W PĘTLI (mm)" Binding="{Binding StandardDisplacement, StringFormat=\{0:N2\}}" Foreground="DarkGreen" FontWeight="Bold"/>
                                <DataGridTextColumn Header="PRZESUW STARTOWY (mm)" Binding="{Binding StartupDisplacement, StringFormat=\{0:N2\}}" Foreground="Gray"/>
                                <DataGridTextColumn Header="OŚ NOŻA (mm)" Binding="{Binding CutTargetX, StringFormat={}{0:N2}}" Width="120" Foreground="Magenta"/>
                                <DataGridCheckBoxColumn Header="CIĘCIE" Binding="{Binding IsCutActive}"/>
                                <DataGridTextColumn Header="STEMPLE (MASK)" Binding="{Binding PunchesMask}"/>
                                <DataGridTextColumn Header="INFO" Binding="{Binding Info}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>

                <TabItem Header="Wizualizacja Detalu (CAD)">
                    <ui:DetailVisualizerControl x:Name="CadVisualizer" />
                </TabItem>
                
            </TabControl>
        </Grid>
        </Grid>
</Window>
